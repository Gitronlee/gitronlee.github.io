<div class="color-tool">
  <div class="picker-section">
    <label>选择颜色</label>
    <div class="picker-row">
      <input type="color" id="colorPicker" value="#3b82f6" />
      <input type="text" id="hexInput" value="#3b82f6" class="hex-input" />
    </div>
  </div>
  
  <div class="preview" id="colorPreview"></div>
  
  <div class="results">
    <label>颜色值</label>
    <div class="color-row">
      <span class="color-label">HEX</span>
      <code id="hexResult" class="color-value">#3B82F6</code>
      <button class="copy-btn" data-target="hexResult">复制</button>
    </div>
    <div class="color-row">
      <span class="color-label">RGB</span>
      <code id="rgbResult" class="color-value">rgb(59, 130, 246)</code>
      <button class="copy-btn" data-target="rgbResult">复制</button>
    </div>
    <div class="color-row">
      <span class="color-label">HSL</span>
      <code id="hslResult" class="color-value">hsl(217, 91%, 60%)</code>
      <button class="copy-btn" data-target="hslResult">复制</button>
    </div>
  </div>
</div>

<script>
  const colorPicker = document.getElementById('colorPicker');
  const hexInput = document.getElementById('hexInput');
  const colorPreview = document.getElementById('colorPreview');
  const hexResult = document.getElementById('hexResult');
  const rgbResult = document.getElementById('rgbResult');
  const hslResult = document.getElementById('hslResult');

  function hexToRgb(hex: string): [number, number, number] {
    const r = parseInt(hex.slice(1, 3), 16);
    const g = parseInt(hex.slice(3, 5), 16);
    const b = parseInt(hex.slice(5, 7), 16);
    return [r, g, b];
  }

  function rgbToHsl(r: number, g: number, b: number): [number, number, number] {
    r /= 255; g /= 255; b /= 255;
    const max = Math.max(r, g, b), min = Math.min(r, g, b);
    let h = 0, s = 0, l = (max + min) / 2;

    if (max !== min) {
      const d = max - min;
      s = l > 0.5 ? d / (2 - max - min) : d / (max + min);
      switch (max) {
        case r: h = ((g - b) / d + (g < b ? 6 : 0)) / 6; break;
        case g: h = ((b - r) / d + 2) / 6; break;
        case b: h = ((r - g) / d + 4) / 6; break;
      }
    }

    return [Math.round(h * 360), Math.round(s * 100), Math.round(l * 100)];
  }

  function updateColor(hex: string) {
    if (!hex.match(/^#[0-9A-Fa-f]{6}$/)) return;
    
    colorPreview.style.background = hex;
    hexResult.textContent = hex.toUpperCase();
    
    const [r, g, b] = hexToRgb(hex);
    rgbResult.textContent = `rgb(${r}, ${g}, ${b})`;
    
    const [h, s, l] = rgbToHsl(r, g, b);
    hslResult.textContent = `hsl(${h}, ${s}%, ${l}%)`;
  }

  colorPicker?.addEventListener('input', (e) => {
    const value = (e.target as HTMLInputElement).value;
    hexInput.value = value;
    updateColor(value);
  });

  hexInput?.addEventListener('input', (e) => {
    let value = (e.target as HTMLInputElement).value;
    if (!value.startsWith('#')) value = '#' + value;
    if (value.match(/^#[0-9A-Fa-f]{6}$/)) {
      colorPicker.value = value;
      updateColor(value);
    }
  });

  document.querySelectorAll('.copy-btn').forEach((btn) => {
    btn.addEventListener('click', function() {
      const target = document.getElementById(this.getAttribute('data-target'));
      navigator.clipboard.writeText(target.textContent);
      this.textContent = '已复制!';
      setTimeout(() => { this.textContent = '复制'; }, 1500);
    });
  });
</script>

<style>
  .color-tool {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }
  
  label {
    font-weight: 500;
    color: var(--text-secondary);
    font-size: 0.875rem;
    display: block;
    margin-bottom: 0.5rem;
  }
  
  .picker-row {
    display: flex;
    gap: 0.75rem;
    align-items: center;
  }
  
  input[type="color"] {
    width: 60px;
    height: 60px;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    background: none;
  }
  
  .hex-input {
    flex: 1;
    background: var(--bg-primary);
    border: 1px solid var(--border-color);
    color: var(--text-primary);
    padding: 0.75rem;
    border-radius: 8px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 1rem;
  }
  
  .hex-input:focus {
    border-color: var(--accent-blue);
    outline: none;
  }
  
  .preview {
    height: 80px;
    border-radius: var(--radius);
    border: 1px solid var(--border-color);
  }
  
  .results {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }
  
  .color-row {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    background: var(--bg-primary);
    padding: 0.75rem;
    border-radius: 8px;
    border: 1px solid var(--border-color);
  }
  
  .color-label {
    min-width: 50px;
    font-weight: 600;
    color: var(--accent-pink);
  }
  
  .color-value {
    flex: 1;
    font-family: 'JetBrains Mono', monospace;
    color: var(--text-primary);
  }
  
  .copy-btn {
    background: var(--bg-tertiary);
    border: none;
    color: var(--text-primary);
    padding: 0.35rem 0.75rem;
    border-radius: 4px;
    cursor: pointer;
    font-size: 0.75rem;
    transition: var(--transition);
  }
  
  .copy-btn:hover {
    background: #475569;
  }
</style>
